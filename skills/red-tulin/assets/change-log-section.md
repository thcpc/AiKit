# 变更记录

<!--
本章节格式由 class-design-template.md 和 function-design-template.md 共享。
create_design.py 在生成设计文档时，会将本章节拼接到模板正文之后。
每次 create-design / change-design 工作流执行后，都在设计文档末尾追加一条记录。
-->

## {{date:YYYY-MM-DD HH:mm:ss}} - {{操作类型}}

- **操作**：{{初始化 / 生成 / 更新}}
- **原因**：变更原因 {{变更 / 关联更新}}
- **ID**：{{ID}}（变更的话，ID 是新生成 ID；关联更新的话，是引发此次变更的 ID。ID 生成规则：UUID）
- **摘要**：一句话总结本次操作
- **变更**：（仅 change-design 记录，create-design 首次生成时填"首次生成"）
  - 新增方法：{{新增的方法列表，无则填"无"}}
  - 修改方法：{{修改的方法列表，无则填"无"}}
      - {{方法名}}
      - **修改类型**：{{部分修改 / 整体重写}}<!-- 部分修改：仅调整方法内部分逻辑/参数/返回值；整体重写：方法的处理逻辑整体替换。对应 make 工作流的"修改已存在方法的部分代码"/"修改已存在方法的所有代码"分支 -->
      - {{变更内容}}
  - 删除方法：{{删除的方法列表，无则填"无"}}
