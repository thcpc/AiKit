---
tags: [代码设计模板]
author: {{hostname}}
created: {{date:YYYY-MM-DD HH:mm:ss}}
updated: {{date:YYYY-MM-DD HH:mm:ss}}
language: [{{language}},{{version}}]
namespace: {{namespace}}
filename: {{filename}}
status: {{status}} (创建时默认状态为 Designing)
---


# 代码设计模板

> 使用说明：
> 1. 按需填写下面的章节，不适用的章节可以删除
> 2. 带 `<!-- -->` 的内容是填写说明，完成后可删除

---




<!-- 如果只有独立函数（没有类），删除"类定义"章节，直接写"函数定义"。-->

## 类定义

### 父类
<!-- 如果没有父类，可以删除 -->

### 类：{ClassName}

<!-- 一句话描述这个类的职责 -->

#### 成员变量

| 变量名 | 类型 | 说明 | 默认值 |
|--------|------|------|--------|
| | | | |

#### 构造函数

**参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| | | | |

**处理**：
<!-- 逐条描述初始化逻辑 -->
1. ...

#### 方法：{methodName}

**参数**：

| 参数名 | 类型 | 必填 | 说明 | 参考文件 |
|--------|------|------|------|---------|
| | | | | <!-- 如果参数结构来自某个文件/接口，填路径 --> |

**处理**：
<!-- 逐条描述处理逻辑 -->
1. ...
2. ...
<!-- ⚠️ 注意：{特殊情况说明} -->

**返回值**：

| 类型 | 说明 |
|------|------|
| | |

<!-- 「变更记录」章节格式统一定义在 assets/change-log-section.md 中，由 create_design.py 自动拼接到本文档末尾，此处不再重复。-->

